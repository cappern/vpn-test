# .env.example
# Copy to .env and fill in values. Do not commit your real secrets.

# Core
VPN_URL=https://vpn.example.com
VPN_USER=alice
VPN_PASS=change-me

# Optional basics
AUTHGROUP=
EXPECT_TIMEOUT=30
SLEEP_INTERVAL=60
FAIL_RETRY_DELAY=0
LOG_PREFIX=[VPN-PROBE]
MFA_MODE=password

# TLS server trust hardening (one is recommended)
SERVERCERT_PIN=
# If you mount a custom PEM (see docker-compose.yaml comment), set its path here:
CA_CERT_BUNDLE=

# --- Client certificate options (pick ONE style below) ---
# 1) PEM pair
CLIENT_CERT=/certs/client.crt.pem
CLIENT_KEY=/certs/client.key.pem
KEY_PASSWORD=

# 2) PKCS#12 bundle (.p12/.pfx) — set CLIENT_CERT to the file and clear CLIENT_KEY
# CLIENT_CERT=/certs/client.p12
# CLIENT_KEY=
# KEY_PASSWORD=your-p12-password

# 3) PKCS#11 token/smart card — set PKCS#11 URL as CLIENT_CERT
# CLIENT_CERT=pkcs11:token=MyToken;id=%01
# PKCS11_PIN=123456

# Optional: Cisco Multi-Certificate Authentication (MCA)
MCA_CERTIFICATE=
MCA_KEY=
MCA_KEY_PASSWORD=

# Warn if client cert is expiring (days):
CERT_EXPIRE_WARNING=

# Extra flags to pass to openconnect (only if you know what you're doing)
OPENCONNECT_EXTRA=

# Silence child output (only JSON + INFO)
QUIET=0
